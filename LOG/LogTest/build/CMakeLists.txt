CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

#PROJECT(AsynLog)
#PROJECT(LogFile)
PROJECT(Logging)

SET(BASEROOT "../../")

SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -I ${BASEROOT} -ggdb -O0 ")

SET(EXECUTABLE_OUTPUT_PATH "${BASEROOT}/LogTest/bin")

SET(MAIN_PATH "${BASEROOT}/LogTest/${PROJECT_NAME}Test.cpp")

INCLUDE_DIRECTORIES("${BASEROOT}/logBase")

AUX_SOURCE_DIRECTORY("${BASEROOT}/logBase" SRC)

MESSAGE("${SRC}")

MESSAGE("${MAIN_PATH}")

ADD_EXECUTABLE("${PROJECT_NAME}" "${SRC}" "${MAIN_PATH}")

TARGET_LINK_LIBRARIES("${PROJECT_NAME}" pthread)